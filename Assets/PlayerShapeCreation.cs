using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PlayerShapeCreation : MonoBehaviour {

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}


    private void CreateShapePeices()
    {
        if(Input.GetAxis("LVertical") > 0f)
        {
            //transform.gameObject
        }
        else if(Input.GetAxis("LHorizontal") > 0f)
        {

        }
            
    }
}
